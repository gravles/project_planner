# Client-side (Vite-inlined). Get values from the Supabase dashboard.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Server-side vars (set in Vercel, not here) are documented in README.md
